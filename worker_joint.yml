---
- name: Join Worker Nodes to Cluster
  hosts: all
  become: true
  tasks:
    - name: Complete Worker Cleanup
      ansible.builtin.shell: |
        kubeadm reset -f
        systemctl stop kubelet
        systemctl stop containerd
        rm -rf /etc/cni/net.d /var/lib/kubelet/* /etc/kubernetes/*
        # Ensure bridge modules are active
        modprobe br_netfilter
        sysctl -w net.ipv4.ip_forward=1
      failed_when: false

    - name: Refresh Containerd Config
      ansible.builtin.shell: |
        rm -f /etc/containerd/config.toml
        containerd config default > /etc/containerd/config.toml
        sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
      notify: Restart containerd

    - name: Flush Handlers
      ansible.builtin.meta: flush_handlers

    - name: Wait for Runtime
      ansible.builtin.pause:
        seconds: 15

    - name: Join Cluster with Verbose Logging
      ansible.builtin.command: >
        kubeadm join 192.168.0.102:6443 
        --token <PUT_NEW_TOKEN_HERE> 
        --discovery-token-ca-cert-hash sha256:<PUT_NEW_HASH_HERE>
        --v=5
      register: join_out

    - name: Debug Join Output
      ansible.builtin.debug:
        var: join_out.stdout_lines

  handlers:
    - name: Restart containerd
      ansible.builtin.systemd:
        name: containerd
        state: restarted
